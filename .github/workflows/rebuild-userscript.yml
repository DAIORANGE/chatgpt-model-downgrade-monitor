name: Rebuild userscript from staged parts

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Staging clean userscript parts; automatic rebuild temporarily paused.'
